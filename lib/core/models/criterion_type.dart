/// Enum representing the type of a criterion.
enum CriterionType {
  /// Discrete criterion with predefined values
  discrete,

  /// Continuous criterion with a range of values
  continuous,
}

